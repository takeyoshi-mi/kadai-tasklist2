<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |tasks| %>
    <li><%= link_to tasks.id, tasks %> : <%= tasks.status %> > <%= tasks.content %></li>
  <% end %>
</ul>

<%= link_to '新しいタスクの投稿', new_task_path %>